# YouTube 下載器

一個基於 Electron + React 的現代化 YouTube 影片/音訊下載工具，支援繁體中文介面。

![YouTube Downloader](https://img.shields.io/badge/Electron-v28-blue)
![React](https://img.shields.io/badge/React-v18-61dafb)
![License](https://img.shields.io/badge/license-MIT-green)

## ✨ 功能特色

- 🎵 **多格式支援**：下載 MP4 影片或 MP3 音訊
- 🌏 **完整中文支援**：介面和檔案名稱完美支援繁體中文
- 📊 **即時進度**：顯示下載進度百分比
- 📝 **下載歷史**：自動記錄下載歷史，可一鍵開啟檔案位置
- ✅ **網址驗證**：自動檢查 YouTube 網址有效性
- 🎨 **現代化介面**：使用 TailwindCSS 打造的美觀 UI
- 🖱️ **智慧右鍵選單**：支援複製、貼上、全選等操作
- 🔍 **檔案檢查**：自動檢查下載檔案是否存在

## 📋 系統需求

- Windows 10/11
- Node.js 16.x 或更高版本
- npm 或 yarn

## 🚀 快速開始

### 1. 安裝依賴

```bash
npm install
```

### 2. 下載必要工具

> **重要提示**：由於檔案體積較大，`ffmpeg.exe` 和 `yt-dlp.exe` 不包含在此專案的 Git 版本控制中。
> 您需要自行下載這些檔案並放置到 `bin/` 資料夾中。

#### yt-dlp
1. 前往 [yt-dlp releases](https://github.com/yt-dlp/yt-dlp/releases/latest)
2. 下載 `yt-dlp.exe`
3. 將檔案放置到專案的 `bin/` 資料夾中

#### ffmpeg
1. 前往 [ffmpeg builds](https://www.gyan.dev/ffmpeg/builds/)
2. 下載 `ffmpeg-release-essentials.zip`
3. 解壓縮後，從 `bin` 資料夾中找到 `ffmpeg.exe`
4. 將 `ffmpeg.exe` 放置到專案的 `bin/` 資料夾中

**完成後的專案結構：**
```
yt_downloader/
├── bin/
│   ├── README.md         # 下載說明
│   ├── yt-dlp.exe       # 需自行下載
│   └── ffmpeg.exe       # 需自行下載
├── src/
└── package.json
```

> 💡 詳細的下載步驟請參考 [`bin/README.md`](bin/README.md)

### 3. 啟動開發模式

```bash
npm run dev
```

### 4. 打包應用程式 (建立 .exe)

```bash
npm run dist
```

此指令會自動執行建置 (`npm run build`) 並進行打包。
打包完成後的安裝檔 (`.exe`) 會在 `release/` 資料夾中。

## 🎯 使用說明

1. **輸入網址**：在輸入框中貼上 YouTube 影片網址
2. **選擇格式**：選擇 MP4（影片）或 MP3（音訊）
3. **選擇位置**：點擊「瀏覽」選擇下載儲存位置
4. **開始下載**：點擊「開始下載」按鈕
5. **查看歷史**：在下載紀錄中可以開啟檔案位置或清除紀錄

## 🛠️ 技術架構

### 前端
- **React 18**：UI 框架
- **TailwindCSS**：樣式框架
- **Lucide React**：圖示庫

### 後端
- **Electron**：桌面應用框架
- **yt-dlp**：YouTube 下載核心
- **ffmpeg**：音視訊處理
- **execa**：進程管理
- **electron-store**：本地資料儲存

### 開發工具
- **Electron Vite**：建置工具
- **ESBuild**：快速打包

## 📁 專案結構

```
yt_downloader/
├── bin/                      # 執行檔（yt-dlp, ffmpeg）
├── src/
│   ├── main/                 # Electron 主進程
│   │   └── index.js
│   ├── preload/              # 預載腳本
│   │   └── index.js
│   └── renderer/             # React 渲染進程
│       ├── src/
│       │   ├── App.jsx       # 主應用元件
│       │   ├── main.jsx      # 入口檔案
│       │   └── assets/
│       │       └── index.css # 全域樣式
│       └── index.html
├── resources/                # 應用資源
├── electron.vite.config.js   # Vite 配置
├── package.json
└── README.md
```

## ⚙️ 配置說明

### 下載設定
- 預設下載位置：系統下載資料夾
- 支援自訂下載位置
- 自動記住上次選擇的格式

### 快捷鍵
- `Ctrl+C`：複製
- `Ctrl+V`：貼上
- `Ctrl+A`：全選
- `Ctrl+X`：剪下

## 🐛 常見問題

### Q: 下載失敗怎麼辦？
A: 請確認：
1. `bin/` 資料夾中有 `yt-dlp.exe` 和 `ffmpeg.exe`
2. 網址是有效的 YouTube 連結
3. 網路連線正常

### Q: 中文檔名顯示亂碼？
A: 本專案已使用 JSON 解析方式完美解決中文編碼問題，應該不會出現亂碼。

### Q: 如何更新 yt-dlp？
A: 從 [yt-dlp releases](https://github.com/yt-dlp/yt-dlp/releases) 下載最新版本，替換 `bin/yt-dlp.exe` 即可。

## 📝 開發筆記

### 編譯主進程
如果修改了主進程程式碼，需要手動重新編譯：

```bash
npx esbuild src/main/index.js --bundle --platform=node --outfile=dist-electron/main/index.js --external:electron --external:electron-store --external:fs-extra
```

### 編譯預載腳本
```bash
npx esbuild src/preload/index.js --bundle --platform=node --outfile=dist-electron/preload/index.js --external:electron
```

## 🤝 貢獻

歡迎提交 Issue 和 Pull Request！

## 📄 授權

MIT License

## 🙏 致謝

- [yt-dlp](https://github.com/yt-dlp/yt-dlp) - YouTube 下載核心
- [FFmpeg](https://ffmpeg.org/) - 音視訊處理
- [Electron](https://www.electronjs.org/) - 跨平台桌面應用框架
- [React](https://react.dev/) - UI 框架
- [TailwindCSS](https://tailwindcss.com/) - CSS 框架

---

**注意**：本工具僅供個人學習和研究使用，請遵守 YouTube 服務條款和相關法律法規。
